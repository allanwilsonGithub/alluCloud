mysql -u root -palluCloudPassword booksdb < book\booksback.sql
mysql -u root -palluCloudPassword filmdb < film\filmback.sql
mysql -u root -palluCloudPassword webdb < web\webback.sql
mysql -u root -palluCloudPassword eventdb < event\eventback.sql
mysql -u root -palluCloudPassword dashdb < dash\dashback.sql
mysql -u root -palluCloudPassword notedb < note\noteback.sql